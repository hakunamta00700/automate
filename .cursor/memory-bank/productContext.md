# productContext.md

## 왜 존재하나
- 반복적인 콘텐츠 처리/요약/전송 같은 작업을 한 번에 자동화하기 위해.
- 로컬에서 실행되는 다양한 AI CLI를 OpenAI API 스타일로 래핑해 다른 도구에서 쉽게 호출하기 위해.

## 사용자 경험 목표
- 명령 한 번(또는 API 호출 한 번)으로 작업이 끝난다.
- 실패 시 원인(타임아웃, 인증, 외부 CLI 실패)이 로그와 에러 메시지로 명확하다.
- 장시간 작업에서도 서버가 멈추지 않고, 취소/타임아웃 시 리소스가 정리된다.

